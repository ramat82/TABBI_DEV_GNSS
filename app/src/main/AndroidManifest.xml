<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">


    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Bluetooth -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/topo_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/topo_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Accueil chantiers = écran de démarrage obligatoire -->
        <activity
            android:name="dz.ogefgef322.gnss.ChantierHomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Écrans GNSS / Levés / Carte (dans MainActivity actuelle) -->
        <activity
            android:name="dz.ogefgef322.gnss.MainActivity"
            android:exported="false" />

        <activity
            android:name="dz.ogefgef322.gnss.AutoSurveyActivity"
            android:exported="false" />

        <activity
            android:name="dz.ogefgef322.gnss.IntersectActivity"
            android:exported="false" />

        <activity
            android:name="dz.ogefgef322.gnss.AreaSurveyActivity"
            android:exported="false" />

        <activity
            android:name="dz.ogefgef322.gnss.TopoPlanActivity"
            android:exported="false" />
    </application>
</manifest>
